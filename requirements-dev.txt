-r requirements.txt
# aioresponses 0.7.x doesn't yet support aiohttp's newer ClientResponse
# signature; pin aiohttp down for the test environment only.
aiohttp<3.10
pytest>=8
pytest-asyncio>=0.23
aioresponses>=0.7
